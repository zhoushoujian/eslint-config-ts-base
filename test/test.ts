export const disaplyName = (params: number) => {
  if (params === 2) {
    params += 1;
  } else if (params > 2) {
    params -= 1;
  }

  let name: string[] = [];
  name = [];

  params += 1 + name.length;
};

// export const ser = {} as IService;

export function getName(params: string, name: string) {
  if (name === '0') {
    return name;
  }

  test();

  return undefined;
}

function test(): number {
  return 1;
}
